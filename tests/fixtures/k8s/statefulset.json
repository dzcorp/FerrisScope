{
  "apiVersion": "apps/v1",
  "kind": "StatefulSet",
  "metadata": {
    "name": "postgres",
    "namespace": "default",
    "uid": "ss-uid",
    "creationTimestamp": "2026-02-01T08:00:00Z",
    "generation": 2
  },
  "spec": {
    "replicas": 3,
    "serviceName": "postgres-headless",
    "selector": { "matchLabels": { "app": "postgres" } },
    "podManagementPolicy": "OrderedReady",
    "updateStrategy": { "type": "RollingUpdate", "rollingUpdate": { "partition": 0 } },
    "template": {
      "metadata": { "labels": { "app": "postgres" } },
      "spec": { "containers": [{ "name": "main", "image": "postgres:17.2" }] }
    }
  },
  "status": {
    "replicas": 3,
    "readyReplicas": 3,
    "currentReplicas": 3,
    "updatedReplicas": 3,
    "availableReplicas": 3,
    "observedGeneration": 2
  }
}
